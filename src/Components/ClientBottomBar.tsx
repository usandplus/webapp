import React from "react";
import { Button, Nav, Col } from 'react-bootstrap';

const ClientBottomBar: React.FC<{ name: string, category: string }> = ({ name, category }) => (
  <div>
  </div>
);

export default ClientBottomBar;